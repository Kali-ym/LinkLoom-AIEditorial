# workspace 成员：根目录 backend + admin + web + agent-console。
# 默认开发 / 构建 / CI / Docker 排除 agent-console（显式用 *:console 脚本）。
packages:
  - '.'
  - 'admin'
  - 'web'
  - 'agent-console'

dangerouslyAllowAllBuilds: true

overrides:
  '@types/react': ^19.2.5
  '@types/react-dom': ^19.2.3
  uuid: ^11.1.1
