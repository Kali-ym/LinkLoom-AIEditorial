/** Default active topic for mock bootstrap. */
export const MOCK_DEFAULT_TOPIC_ID = 'skills';

/** Default active agent id resolved at seed time from agent list. */
export const MOCK_FALLBACK_AGENT_ID = 'topic_copilot';
